while wait(0.3) do
	for i, pumpkin in pairs(game:GetService("Workspace").PumpkinHunt.spawnedPumpkins:GetDescendants()) do
		if pumpkin:IsA("MeshPart") then
		    warn("----------------------------PUMPKINS ARE HERE!----------------------------")
		end
    end
end
